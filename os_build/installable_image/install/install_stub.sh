#!/usr/bin/env bash
# Prototype install stub — documents install path only. Not a production installer.
set -euo pipefail

echo "GunnchOS installable image prototype — install stub"
echo "Status: prototype only — not for production deployment"
echo ""
echo "This script documents the intended install flow:"
echo "  1. Verify CHECKSUMS.sha256"
echo "  2. Extract gunnchos-installable-image-prototype.tar.gz"
echo "  3. Run launcher from extracted bundle (dev/kiosk host)"
echo ""
echo "No system partition writes are performed by this stub."
