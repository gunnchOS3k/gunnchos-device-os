# CX2F Linux lab — DRM Weston + shell render/capture

Isolated from Device Lab. Clones CX2E overlay as backing file (CX2E immutable).

```bash
python -m gunnchos_device_os.cx2f --prepare-only
python -m gunnchos_device_os.cx2f --full-lifecycle
```

Evidence: `artifacts/complete_experience/cx2f/`
