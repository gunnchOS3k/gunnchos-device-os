server {
    listen 80;
    server_name _;
    root /gunnchos/apps/launcher_mock/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /health {
        return 200 'GunnchOS Phase 0 OK\n';
        add_header Content-Type text/plain;
    }
}
