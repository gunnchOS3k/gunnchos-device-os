# Validation Center UI

Open `index.html` locally or:

```bash
PYTHONPATH=. python -m gunnchos_device_os.cx4_validation_center serve-static
```

```bash
cd apps/validation_center && npm install && npm test && npm run build && npm run typecheck
```
