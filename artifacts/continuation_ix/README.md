# Continuation IX artifacts

Generated by `make cont-ix-evidence` / Cont IX CI after clean-env package install.

Do not treat file presence alone as readiness. Tokens are earned only when
`artifacts/continuation_ix/DIGITAL_RELEASE_LOCK.json` reports `ok: true` with
empty `blockers.DIGITAL` (or exact failure reasons when not empty).
