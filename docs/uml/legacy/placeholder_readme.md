# Legacy — placeholder UML README

Until this pack, `docs/uml/README.md` was:

> UML. See repo-specific architecture docs. SpectrumX: full UML pack.

That sentence is not architecture. Current diagrams live in `docs/uml/current/`.
The three files under `docs/diagrams/*.mmd` were equally stubbed (`school --> play`
only) and are superseded by `current/state_mode.md`, `current/component.md`, and
`current/activity_update_recovery.md`.
