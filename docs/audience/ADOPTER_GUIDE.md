# Adopter Guide

Build apps for gunnchOS using the Adopter SDK.

1. Copy `sdk/samples/app_template`
2. Declare permissions and device roles in the app manifest
3. Negotiate API versions (`app_manifest`, `permissions`, `device_role`, …)
4. Prefer offline-first behavior; sync when connectivity returns

See `sdk/README.md` for install and test commands.
