# Integrator Guide

Integrate first-party and adopter apps with gunnchOS services: permissions, device roles, ring input, AI, connectivity, telemetry, fleet, and save/cloud contracts.

Use the Adopter SDK under `sdk/` and negotiate API versions before calling surfaces.
