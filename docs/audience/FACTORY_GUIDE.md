# Factory Guide

Run the digital factory test station:

```bash
make factory-station
```

Each SKU suite records serial, HW revision, SW revision, pass/fail, measurements with limits, timestamp, station id, and operator id. Production private keys must never be stored in this repository.
