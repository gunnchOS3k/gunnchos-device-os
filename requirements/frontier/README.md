# gunnchOS frontier requirements (owner mirror)

Canonical control plane: `gunnchos-7gc-ai-ran-field-kit/program/frontier_parity/`.

This directory mirrors:

- `gunnchOS_FRONTIER_OS_PARITY_REQUIREMENTS.md` — normative Parts A–U
- `COMPETITIVE_GAP_MASTER_REGISTER.md` — local reference to the OS↔AI integration layer

Frontier tokens remain false until earned (`GUNNCHOS_FRONTIER_OS_PARITY=false`). Do not treat presence of these files as completion evidence.
