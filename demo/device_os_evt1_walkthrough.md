# EVT-1 Device OS Alpha Walkthrough

1. Select device profile `Student14`
2. Select user profile `student`
3. Enter `School` mode — Steam blocked
4. Run `python3 scripts/run_device_os_demo.py`
5. Switch to `Developer` — VS Code/WSL on allow list
6. Switch to `Play` — Steam placeholder allowed
7. Switch to `Media` — browser routes for streaming (DRM caveats)
8. Export privacy-safe telemetry (opt-in mock)
9. Simulate update + rollback via demo JSON

**Not claimed:** shipping OS, production secure boot, fleet MDM.
